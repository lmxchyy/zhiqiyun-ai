# 模型供应商与计费管理实施说明

- 平台管理员创建模型供应商，保存编码、名称、服务地址、状态和非敏感配置。
- 平台管理员配置模型编码、能力范围、会员等级和每种能力的积分单价。
- 生成工作台、模型权限校验和积分估算统一使用动态模型配置。
- 模型停用后立即从用户可用模型目录移除，相关计费规则同步停用。
- 模型供应商与模型配置变更均记录审计日志。
- API Key 不通过管理页面以明文持久化，真实供应商密钥继续由 Docker Secret 或环境变量提供。

主要接口：

- `GET /api/v1/admin/model-config`
- `POST /api/v1/admin/model-providers`
- `POST /api/v1/admin/model-definitions`
- `POST /api/v1/admin/model-definitions/{id}/status`

Docker Compose 启动时会自动执行 `019-model-pricing-rules.sql`。
