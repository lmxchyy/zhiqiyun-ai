# 认证会话实施说明

## 已实现能力

- 登录同时签发短期 Access Token 与长期 Refresh Token。
- Refresh Token 在服务端仅保存 SHA-256 摘要。
- 刷新会话时同时轮换 Access Token 和 Refresh Token，旧令牌立即失效。
- 用户退出登录后撤销当前会话。
- 被冻结用户无法登录、刷新或继续使用已有会话。
- Web 前端收到 `401` 后自动刷新会话并重试原请求一次。

## API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/v1/auth/login` | 登录并获取双令牌 |
| `POST` | `/api/v1/auth/refresh` | 轮换访问令牌和刷新令牌 |
| `POST` | `/api/v1/auth/logout` | 撤销当前会话 |

Docker Compose 启动时会自动执行 `014-auth-sessions.sql`，创建认证会话领域表和活跃会话索引。
