# RabbitMQ's 50 MB default is intended for development environments.
# Keep a production-sized buffer so publishers are blocked before the host
# reaches a state where database, object-storage, and Docker writes can fail.
disk_free_limit.absolute = $(RABBITMQ_DISK_FREE_LIMIT)
