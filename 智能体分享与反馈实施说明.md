# 智能体分享与反馈实施说明

## 已实现能力

- 已发布智能体生成唯一公开分享令牌。
- 外部用户无需登录即可通过分享接口调用智能体。
- 公开调用与登录用户调用分别统计。
- 登录用户可对自己的调用提交 1 至 5 分评价和文字反馈。
- 智能体所有者可查看调用次数、公开调用数、平均响应时间、Token 使用、成本和平均评分。
- 重复创建分享链接返回当前有效链接，避免多个有效入口失控。

## API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/v1/agents/{id}/share` | 创建智能体分享链接 |
| `POST` | `/api/v1/public/agents/{token}/call` | 无需登录公开调用 |
| `GET` | `/api/v1/agents/{id}/stats` | 查询调用和反馈统计 |
| `POST` | `/api/v1/agent-calls/{id}/feedback` | 提交调用反馈 |

Docker Compose 启动时会自动执行 `017-agent-sharing-feedback.sql`。
