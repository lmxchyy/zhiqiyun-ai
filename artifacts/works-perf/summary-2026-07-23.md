# 作品页性能诊断与回归摘要（2026-07-23）

环境：同一套本地 Docker PostgreSQL 数据；旧服务 `:3100`，当前代码的隔离回归服务在容器内 `:3199`。

## 改造前

- 接口：`GET /api/v1/assets?paged=true&limit=4&offset=0&lightweight=true&includeSummary=false`
- 连续请求：10 次，0 次失败
- 响应体：平均 5,322,290 bytes
- 耗时：min 5,251ms；median 6,570ms；P95/max 7,006ms
- 主要体积：4 条记录的 `metadata.sourceUrl` 仍携带 Base64 原图，最大单条 metadata 约 2.65MB

## 当前代码

- 接口：`GET /api/v1/works/recent?limit=20`
- 连续请求：10 次，0 次失败
- 响应体：每次 180,572 bytes
- 服务端总耗时：min 89.650ms；median 163.885ms；P95/max 205.333ms
- 数据存取段：min 88.165ms；median 162.187ms；P95/max 202.613ms
- 原始服务端结构化日志：`api-container-final.log`

## 数据库

- 已存在：`idx_xz_assets_user_active_created (user_id, created_at DESC, id DESC) WHERE deleted_at IS NULL`
- 已存在：`idx_xz_assets_user_created (user_id, created_at DESC, id DESC)`
- 已存在：`idx_xz_assets_tenant_user (tenant_id, user_id, created_at DESC)`
- 本地 115 条当前用户作品下，最近 ID 筛选执行计划耗时 0.671ms；完整接口耗时主要来自兼容旧记录时对巨大 JSONB metadata 的解压和少量卡片字段提取。

## 未完成的运行时项

微信开发者工具已经打开最终构建，但自动化连接在写入登录态时连续出现 `timeout waiting for automator response`。因此没有把“真机连续进入 10 次均小于 100ms”标记为通过；失败进度与堆栈保存在 `../wechat-works-performance/progress.log` 和 `../wechat-works-performance/run.log`。
