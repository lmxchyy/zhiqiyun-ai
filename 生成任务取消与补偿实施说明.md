# 生成任务取消与补偿实施说明

- 用户可取消处于 `QUEUED` 或 `RETRYING` 状态的生成任务。
- 已进入处理、成功、失败或已取消的任务不能再次取消。
- 取消后任务进入 `CANCELLED` 状态并记录取消时间和原因。
- 个人积分任务取消后自动退还冻结积分。
- 企业额度任务取消后自动返还成员额度并记录额度流水。
- 取消操作写入审计日志，保留计费来源和返还积分信息。

接口：`POST /api/v1/generation-tasks/{id}/cancel`
