# 生成任务与作品闭环实施说明

## 已实现能力

- 生成任务创建前积分估算。
- 生成请求业务幂等键，重复提交不会重复创建任务或扣除积分。
- 失败任务由原用户重新提交，保留 `retryOfTaskId` 来源追踪。
- 作品收藏与取消收藏。
- 作品逻辑删除，历史任务、积分流水和模型调用记录保持可追溯。
- 生成作品再次生成。
- 通过鉴权接口下载 MinIO 中的作品文件。

## API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/v1/generations/estimate` | 计算预计积分消耗 |
| `POST` | `/api/v1/generations/...` | 创建任务，支持 `Idempotency-Key` 请求头或 `idempotencyKey` 字段 |
| `POST` | `/api/v1/generation-tasks/{id}/retry` | 重试失败任务 |
| `POST` | `/api/v1/assets/{id}/favorite` | 更新收藏状态 |
| `POST` | `/api/v1/assets/{id}/regenerate` | 根据作品再次生成 |
| `DELETE` | `/api/v1/assets/{id}` | 逻辑删除作品 |
| `GET` | `/api/v1/assets/{id}/download` | 下载作品文件 |

## Docker 数据迁移

Docker Compose 启动时会自动执行 `012-generation-asset-lifecycle.sql`，为生成任务增加重试来源字段，为作品增加逻辑删除字段及活跃作品索引。

## 验证范围

- 相同幂等键只创建一个任务并只扣费一次。
- 仅失败任务允许重试。
- 重试任务保留原任务来源。
- 收藏、再次生成和逻辑删除遵守用户数据隔离。
- 逻辑删除后作品不再出现在作品中心。
