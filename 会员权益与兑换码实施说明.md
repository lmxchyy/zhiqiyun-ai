# 会员权益与兑换码实施说明

## 已实现能力

- 管理员创建固定金额或折扣比例优惠券。
- 用户领取优惠券，下单时锁定并抵扣订单金额。
- 支付后优惠券标记为已使用，订单退款后自动恢复。
- 管理员、一级代理商和二级代理商创建积分或会员兑换码。
- 用户兑换积分或会员权益，同一用户不能重复兑换同一兑换码。
- 使用代理商邀请码注册时，新用户和邀请代理商分别获得邀请积分奖励。

## 权益规则

- 邀请注册新用户获得 20 积分。
- 邀请代理商获得 50 积分。
- 代理商创建单个积分兑换码时，积分额度不得超过 10,000。
- 优惠券和兑换码均校验状态、有效期、使用次数和用户使用记录。
- 优惠券金额和规则会写入订单快照，后续规则变更不影响历史订单。

## API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/v1/coupons` | 管理员创建优惠券 |
| `POST` | `/api/v1/coupons/claim` | 用户领取优惠券 |
| `GET` | `/api/v1/coupons` | 查询我的优惠券 |
| `POST` | `/api/v1/redemption-codes` | 管理员或代理商创建兑换码 |
| `GET` | `/api/v1/redemption-codes` | 查询可管理兑换码 |
| `POST` | `/api/v1/redemption-codes/redeem` | 用户兑换权益 |

Docker Compose 启动时会自动执行 `013-membership-benefits.sql`。
